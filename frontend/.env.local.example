# Network RPC URL
NEXT_PUBLIC_RPC_URL=http://127.0.0.1:8545

# Contract Addresses (update after deployment)
NEXT_PUBLIC_BUG_BOUNTY_PROGRAM=
NEXT_PUBLIC_BOUNTY_VAULT=
NEXT_PUBLIC_CONFIDENTIAL_PAYOUTS=
NEXT_PUBLIC_VERIFIER=
NEXT_PUBLIC_WHITEHAT_REPUTATION=
NEXT_PUBLIC_DISPUTE_RESOLVER=
NEXT_PUBLIC_PROGRAM_REGISTRY=
NEXT_PUBLIC_MOCK_USDT=

# Admin RSA Public Key (hex, for encrypting symmetric keys)
# Generate with: node -e "const crypto = require('crypto'); const { publicKey } = crypto.generateKeyPairSync('rsa', { modulusLength: 2048, publicKeyEncoding: { type: 'spki', format: 'der' } }); console.log(publicKey.toString('hex'));"
NEXT_PUBLIC_ADMIN_RSA_KEY=

# Chain ID (31337 for Hardhat local)
NEXT_PUBLIC_CHAIN_ID=31337
